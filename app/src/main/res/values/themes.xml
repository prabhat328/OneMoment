<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.OneMoment" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_ivory</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_blank</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_ivory</item>
        <item name="postSplashScreenTheme">@style/Theme.OneMoment</item>
    </style>

</resources>